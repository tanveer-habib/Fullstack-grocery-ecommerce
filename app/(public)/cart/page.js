import Cart from "@/components/Cart.jsx";

const CartPage = () => {
    return (
        <Cart />
    );
};

export default CartPage;

export const metadata = {
    title: "Grocery - Cart",
    description: "Grocery - This is home page"
};