import Address from "@/components/Address.jsx";

const AddAddressPage = () => {
    return (
        <Address />
    );
};

export default AddAddressPage;

export const metadata = {
    title: "Grocery - Add address",
    description: "Grocery - This is add address page"
};